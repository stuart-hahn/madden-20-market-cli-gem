RSpec.describe Madden20MarketPrices do
  it "has a version number" do
    expect(Madden20MarketPrices::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
